import React, { Component } from "react";
import { View, Text } from "react-native";

class TaskItem extends Component {
    render() {
        return <Text>just a test</Text>
    }
}

export default TaskItem